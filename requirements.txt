# =============================================================================
# requirements.txt - generic CPU environment (Windows / laptop)
# =============================================================================
# Install with:
#   pip install -r requirements.txt
#
# Python 3.11+ recommended.
# For the DGX Spark GPU environment see requirements-dgx.txt.
# =============================================================================

# --- Core data ---
numpy>=1.24
pandas>=2.0

# --- Visualisation ---
matplotlib>=3.7
seaborn>=0.12

# --- Machine learning ---
scikit-learn>=1.3,<2.0
imbalanced-learn>=0.11       # SMOTE support (notebook 04)

# --- Jupyter ---
jupyterlab>=4.0
ipykernel>=6.0               # registers the venv as a Jupyter kernel

# --- Notebook execution ---
papermill>=2.4               # run notebooks headlessly from the command line

# --- Dataset download ---
certifi>=2024.0              # SSL certificates for setup.py download
